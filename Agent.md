Create an eye-tracking system in React that uses the device’s webcam. The tracker must work with any camera and on any device (desktop, laptop, or tablet). Calibration must be simple and accessible for users with motor impairments. The system must compensate for head movement — including rotation, tilt, flexion/extension, and changes in distance from the camera — without causing the tracking cursor to drift or lose accuracy.
